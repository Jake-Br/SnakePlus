using System;

namespace SnakePlus {
    class Program {
        static void Main(string[] args) {
            GameEngine engine = new GameEngine(); // create engine object
            engine.start(); // start the engine
        }
    }
}
